name: tsood
channels:
  - conda-forge
dependencies:
  - python=3.14
  - pip
  - pip:
      - -r requirements.txt
# Note: torch is the CPU build (torch==2.12.1+cpu). If conda/pip cannot resolve it,
# install separately:  pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cpu
